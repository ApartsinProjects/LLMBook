A clear vertical block diagram of a decoder-only Transformer architecture, drawn top-to-bottom on a portrait canvas. Bottom of diagram: input row of small token boxes labeled 'Token Embedding' summed (with a plus symbol) with 'Position Embedding'. Above that: a tall outlined rectangular container labeled at the top-right corner 'N x' (indicating the block is repeated N times), with one ghost/translucent duplicate rectangle behind it suggesting stacking. Inside this container, drawn as a vertical sub-stack with arrows: (1) a small box 'LayerNorm', (2) a box 'Causal Multi-Head Self-Attention', (3) a circled-plus residual junction labeled 'residual add' where a curved arrow bypasses steps 1-2, (4) a small box 'LayerNorm', (5) a box 'Feed-Forward Network (MLP)', (6) a circled-plus residual junction with a second curved bypass arrow. Above the repeated block: a single box 'Final LayerNorm', then 'Linear (Vocab Projection)', then 'Softmax', and at the very top a row of small output boxes labeled 'Next-Token Probabilities'. Use a coherent palette: deep navy for outlines, amber for residual paths, teal for attention block, soft red for FFN, light gray for embeddings. The two residual paths must be drawn as curved arrows wrapping around their sub-layers, NOT as separate boxes. Show clearly that the residual add is a junction, not a layer.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.