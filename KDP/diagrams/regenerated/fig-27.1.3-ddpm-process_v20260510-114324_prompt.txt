Diagram of the forward (noising) and reverse (denoising) processes of a Denoising Diffusion Probabilistic Model (DDPM). Two horizontal arrows of image thumbnails laid out as a Markov chain. Top arrow points right and is labeled 'Forward process q(x_t | x_{t-1}) — fixed Gaussian noise schedule'. It shows five thumbnails: x_0 (a clear small picture of a stylized object), x_1 (slightly noisy), x_t (very noisy), x_{T-1} (almost noise), x_T (pure Gaussian noise, gray static). Below each thumbnail show a small Greek letter beta_t (noise variance) tag. Bottom arrow points left (reverse direction) and is labeled 'Reverse process p_theta(x_{t-1} | x_t) — learned denoiser'. Each step is annotated with a small neural network icon labeled 'epsilon_theta'. Title at top: 'Diffusion Model: Forward and Reverse Markov Chains'. Use deep navy for forward, teal for reverse. Show clear progression from sharp image on left to pure noise on right. Crisp, clean, no decoration.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.