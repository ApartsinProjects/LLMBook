A clear technical diagram of a single LSTM cell's internal structure on a landscape canvas. The cell is drawn as a large rounded rectangle representing the cell boundary at time step t. Inputs entering from the left: 'x_t' (current input) on the lower left, 'h_(t-1)' (previous hidden state) on the upper left. The previous cell state 'C_(t-1)' enters from the far upper-left and flows along a horizontal green-highlighted line at the top labeled 'Cell State (memory highway)' that exits on the upper-right as 'C_t'. Three gates drawn as small sigmoid units (labeled with sigma symbol) with their outputs as labeled arrows pointing UP toward the cell-state highway. From left to right: (1) Forget Gate (sigma) - its output is multiplied (drawn as a circled-x) into the cell-state line, label 'forget gate f_t'. (2) Input Gate (sigma) and a candidate-values unit (tanh) - their outputs are multiplied together (circled-x) and then added (circled-plus) to the cell-state line, labels 'input gate i_t' and 'candidate C_tilde_t'. (3) Output Gate (sigma) on the right - the cell-state value is passed through a tanh, then multiplied (circled-x) by the output gate, producing the new hidden state 'h_t' that exits the cell on the lower right (and also as a copy on the upper right). Use color coding: green for the cell-state highway, amber for gates, teal for the tanh/candidate path, gray for inputs/outputs, deep-navy outlines, white background. Show pointwise operations (circled-x, circled-plus) clearly as labeled small circles, not as boxes.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.