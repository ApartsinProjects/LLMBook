A clear technical diagram of multi-head attention with h=4 heads on a portrait canvas. Bottom: a single input row of token vectors labeled 'Input X (sequence of d_model vectors)'. From this input, three downward-fanning sets of arrows go into 12 small projection boxes arranged in a 3-column grid (one column for Q, one for K, one for V), with 4 rows (one per head). The columns are labeled at the top: 'W^Q_i', 'W^K_i', 'W^V_i', and rows are labeled 'Head 1', 'Head 2', 'Head 3', 'Head 4'. After each row's three projections, an arrow leads into a per-head box labeled 'Scaled Dot-Product Attention' (4 such boxes, one per head, drawn vertically stacked or side-by-side). Each attention box outputs a small vector labeled 'head_i'. The 4 head outputs feed into a single horizontal 'Concat' bar. Above the Concat bar, an arrow goes into a final box labeled 'W^O (Output Projection)'. Top of diagram: an arrow labeled 'Output (d_model)' leaves the W^O box. Use a coherent palette: each head gets the same hue but different shade (e.g. four shades of teal); Q in light teal, K in medium teal, V in dark teal; W^O in amber; concat bar in gray; deep navy outlines; white background. Show clearly that Q, K, V projections are independent per head (different weight matrices), and that all heads run in parallel before the concat-and-project step.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.