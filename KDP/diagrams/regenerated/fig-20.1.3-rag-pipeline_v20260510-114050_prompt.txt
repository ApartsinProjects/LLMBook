Architecture diagram of a Retrieval-Augmented Generation (RAG) pipeline split into two horizontal lanes that share a central vector database. Top lane labeled 'Ingestion (offline)': Documents (folder icon) -> Chunker -> Embedding Model -> Vector Database (cylinder). Bottom lane labeled 'Query (online)': User Query (speech bubble) -> Embedding Model -> Retriever (arrow into the vector DB) -> Top-k Chunks -> Prompt Builder (combines chunks plus query) -> LLM -> Answer (response bubble). Both lanes converge on the central vector database in the middle of the figure to make storage versus retrieval roles obvious. Each box uses a small distinguishing icon (document, scissors for chunker, gear for embedding, magnifying glass for retriever, brain for LLM). Use thin directional arrows. Two-color lane backgrounds: pale teal for ingestion, pale amber for query. White overall background. Title at top: 'RAG Pipeline'. No three dimensional perspective.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.