A side-by-side comparison diagram of two Transformer block variants on a landscape canvas. Two columns separated by a vertical divider. Left column titled 'Post-LN (Original Transformer, 2017)'. Right column titled 'Pre-LN (Modern Transformers, GPT-2 onward)'. Each column contains a single Transformer sub-layer block drawn vertically with labeled boxes and arrows. LEFT (Post-LN) flow from bottom up: input arrow labeled 'x' enters; box 'Sub-layer (Attention or FFN)'; a curved residual arrow bypasses the sub-layer from input to a circled-plus junction (labeled 'residual add'); above the junction: box 'LayerNorm'; output arrow labeled 'output'. RIGHT (Pre-LN) flow from bottom up: input arrow labeled 'x' enters; box 'LayerNorm'; box 'Sub-layer (Attention or FFN)'; a curved residual arrow bypasses BOTH the LayerNorm and the sub-layer from the original input to a circled-plus junction (labeled 'residual add'); output arrow labeled 'output'. Beneath each column, a small caption: left says 'LN(x + Sublayer(x))', right says 'x + Sublayer(LN(x))'. Use deep navy outlines, amber residual arrows, teal sub-layer boxes, soft gray LayerNorm boxes, white background. Make the residual arrow path geometry visually different between the two sides so the viewer immediately sees Pre-LN's residual stays outside the norm.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.