Diagram of Byte-Pair Encoding (BPE) tokenizer training procedure shown as a vertical sequence of four numbered stages. Stage 1: 'Initialize' - a small box of single character tokens from the corpus, e.g. 'l, o, w, e, r, n, s, t' as separate tiles. Stage 2: 'Count pairs' - the same tokens shown sliding into adjacent pairs with a frequency table on the side: ('l','o') 5, ('o','w') 5, ('e','r') 4, ('n','e') 3. Stage 3: 'Merge most frequent pair' - an arrow merging the pair 'l' + 'o' into a new combined token tile labeled 'lo'. Stage 4: 'Repeat until vocabulary size reached' - small loop arrow back to Stage 2 plus a vocabulary growth chart showing tokens added over merges. Use simple flat tiles with monospaced characters inside. Each stage labeled with a numbered circle on the left. Vertical top-to-bottom flow with thin arrows between stages. Include a small final box at bottom labeled 'Final vocabulary: bytes + learned merges'.

Visual style: Technical infographic for an AI textbook (Kindle e-book), modern flat-vector style, clean white background, max 6 distinct colors (use a coherent palette: deep navy, teal, amber/gold, soft red, light gray, white), crisp sans-serif labels, thin precise arrows with clear directionality, well-spaced layout. AVOID: photorealism, clipart, decorative elements, 3D perspective, drop shadows, gradients on text, labels exceeding 4 words, horizontal flow when concept is sequential top-to-bottom, boxes that contain only generic text without an icon or symbol. Render all label text crisply and spelled exactly as listed.