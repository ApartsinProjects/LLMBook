# EPUB build dependencies
# Install with: python -m pip install -r requirements.txt
ebooklib>=0.18
beautifulsoup4>=4.12
lxml>=5.0
Pillow>=10.0
PyYAML>=6.0
Pygments>=2.18  # Server-side syntax highlighting for code blocks (Prism.js doesn't run in EPUB)
google-genai>=1.0  # For Gemini cover image generation (KDP/cover/generate_cover_gemini.py)
