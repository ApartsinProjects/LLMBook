# Pygments Indent Fix Report

_Generated by `scripts/_fix_pygments_indent.py` on 2026-05-16._

## Summary
- HTML files scanned: **536**
- Files modified: **61**
- Files skipped (modified within last 5 min): **0**
- Pygments-highlighted Python blocks scanned: **1387**
- Blocks already valid (skipped): **1201**
- Blocks not indent-loss (skipped, other syntax error / mislabeled / pseudocode): **73**
- Blocks fixed: **105** (target: all 112 audit-flagged + extras my detector caught)
- Blocks unsalvageable (indent-loss but re-indent ambiguous): **8**

## Approach

Walks all HTML files (excluding `node_modules`, `.git`, `KDP`, `build`,
`temp_ebook`, `temp_epub`, `source_fix_backups`, `pagefind`, `templates`,
`.claude`, `.book-update`). For each `<pre><code class="...lang-python
pygments-highlighted...">` block:

1. Strips pygments `<span>` tags to recover plain Python source.
2. Runs `ast.parse` to check validity. If it already parses, skip.
3. Confirms the failure looks like indent-loss (a `:`-ending compound
   opener followed by a same-or-shallower line). If not, skip.
4. Applies `smart_reindent`: paren/triple-string-aware walker that
   re-derives indentation depth from `:`-terminated compound openers.
   Heuristics: sibling-method dedent (flat-source `def` inside a class
   context dedents to class body), after-`return`/`raise`/`continue`
   /`break`/`yield` dedent, decorator alignment, multi-line `def`/`class`
   handled by tracking pending opener until paren closes.
5. Validates that the result re-parses with `ast.parse`. If yes,
   re-pygmentizes via `pygments.highlight(..., HtmlFormatter(nowrap=True))`
   and substitutes the block. If not, leaves the block unchanged.

## Fix strategies used
- `smart-reindent`: **105** blocks (paren-aware Python indent re-derivation)

`autopep8` was tried first but rarely produced a parsing result on raw
indent-lost source. `smart-reindent` succeeded on every salvageable case.

## Verification
- Re-scanned all 1387 pygments-highlighted Python blocks after fix:
  **1306 now parse** (was 1201 before fix).
- Remaining 81 failures: 8 indent-loss unsalvageable (below) plus 73
  blocks in other audit categories (mislabeled shell/yaml/markdown,
  broken f-strings, structurally scrambled examples), which are out of
  scope for this task.

## Unsalvageable indent-loss blocks (8 — left unchanged)

These have genuine structural issues beyond indent loss (scrambled code,
spurious `{...}` brace corruption, `elif` after `else`, etc.):

- `part-8-evaluation-production/module-35-production-engineering/section-35.8.html:152` — retry-with-backoff `RetryConfig` block with `continue`/`raise`/`return` stack scrambled
- `part-8-evaluation-production/module-35-production-engineering/section-35.8.html:387` — same series, fallback-chain block
- `part-8-evaluation-production/module-35-production-engineering/section-35.8.html:560` — same series, try/except scrambled
- `part-8-evaluation-production/module-35-production-engineering/section-35.8.html:713` — same series, circuit-breaker scrambled
- `part-4-training-adapting/module-18-fine-tuning-fundamentals/section-18.7.html:213` — `elif` after `else` (structural error)
- `part-4-training-adapting/module-20-alignment-rlhf-dpo/section-20.1.html:374` — nested control-flow corruption
- `part-10-idea-to-product/module-47-scaling-economics/section-47.3.html:927` — spurious `{name}` braces inserted into identifiers (`def sensitivity_{analysis}`)
- `part-10-idea-to-product/module-48-shipping-deploying/section-48.6.html:63` — control-flow scrambled

These cases should be handled by re-running the upstream source
preprocessor for the affected sections, not by line-by-line indent
inference.

## Notes
- All fixes are conservative: a block is only replaced when the re-indented
  source parses cleanly with `ast.parse`.
- The mtime guard (skip files modified in last 5 minutes) was hit 0 times
  during this run (no concurrent edits detected).
- Script: `scripts/_fix_pygments_indent.py`.
